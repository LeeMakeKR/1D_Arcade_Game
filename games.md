# 1D Arcade Game — 게임 정리

## 1. 구현된 게임 (v1)

| 구분 | 게임 | 모듈 | 요약 |
|------|------|------|------|
| 1P | Snake Invader | `SnakeInvader.h` | 다가오는 멀티컬러 지렁이를 색 매칭 버튼으로 격추 |
| 1P | Solo Pong | `SoloPong.h` | 코트 끝 타격 영역에서 버튼으로 공을 반사, 친 위치에 따라 속도가 갈림 |
| 2P | Duel Pong | `DuelPong.h` | 보드 두 대를 ESP-NOW로 묶어 양 끝에서 공을 주고받는 대전 |

메뉴 표기는 `SNAKE` / `SOLO PONG` / `DUEL PONG`이다(LCD 폭 때문에 Snake Invader는 줄여 쓴다).

---

## 2. 구현된 게임 알고리즘

아래는 설계안이 아니라 현재 코드가 실제로 하는 일이다. 상수 이름은 각 모듈 헤더 상단의
`constexpr` 이름과 같다.

### 공통 구조

**모듈 규약** — 세 게임 모두 같은 세 함수만 노출하고, `1DarcadeV1.0.ino`가 이것만 부른다.
- `begin()` — 진입할 때 한 번. 상태 초기화
- `update(now)` — 매 loop마다. 게임 로직 + LED/LCD 갱신 (`now`는 `millis()`)
- `wantsExit()` — true가 되면 메뉴로 되돌린다
- Duel Pong만 `end()`가 추가로 있다(ESP-NOW 콜백/피어 정리)

**공유 계층은 `Arcade.h`뿐이다.** 스트립·LCD 객체, 스위치 입력(`Input::takePressed()`),
설정값(밝기/볼륨/ESP-NOW 채널), 색 유틸, 코트 좌표 계산(`depthFrom` / `zoneOfDepth` /
`zoneColor` / `halfOwner`)을 여기서 가져다 쓴다. 게임 로직 자체를 공유하는 프레임워크는 없다.

**그 외**
- 범용 1D 스프라이트 타입은 없다. 각 게임이 자기 상태를 직접 들고 있다.
- 이벤트 시스템도 없다. 소리는 게임이 판정한 자리에서 `soundPlay(SFX_*)`를 바로 부른다.
- 렌더는 게임이 `strip.setPixelColor()`로 직접 그리고 `safeShow()`로 내보낸다. 주기는
  `FRAME_MS`(16ms, 약 60FPS)이고, `show()`는 논블로킹이 아니라 176픽셀에 약 5.3ms 걸린다.
- 물리는 고정 타임스텝이 아니라 가변 dt다. 연출의 `delay()`로 프레임이 벌어졌을 때
  공이 튀지 않도록 dt를 0.05초로 잘라 쓴다.
- LCD는 프레임버퍼가 없어 한 장 전송에 약 14ms 걸리므로, 내용이 실제로 바뀐 순간에만 그린다.
- 코트는 라인 LED 172칸(인덱스 4~175)이고, 인덱스 0~3은 스위치 내장 LED다.

**타격 영역(두 퐁 공유, `Arcade.h`)** — 각 끝에서 안쪽으로 18칸.
자기 끝에서부터 빨강 3칸 / 노랑 6칸 / 초록 9칸.

### 2.1 Snake Invader

**상태**
- `snakeColors[]`: 세그먼트 색 배열. index 0 = 선두(플레이어에 가장 가까운 쪽), 최대 100칸
- `headPos`: float, 플레이어 방향으로 `snakeSpeed`씩 전진
- `bullets[8]`: `{ active, pos, color }`. 속도는 전부 `BULLET_SPEED`(150칸/초)로 같다

**루프**
1. 입력: 해금된 스위치 press → 그 스위치 색 총알 생성(`SFX_SHOOT`). 8발이 다 날아가는 중이면 조용히 버린다
2. 지렁이 전진(한 칸 넘어갈 때마다 `SFX_SNAKE_STEP`), 총알 전진
3. 충돌 판정: 총알 pos ≥ 선두 세그먼트 pos — 총알은 어느 쪽이든 소멸한다
   - 색 일치 → 선두 제거, **+10점**, `SFX_BULLET_HIT`
   - 색 불일치 → **지렁이 속도 ×1.15**(`SNAKE_MISS_ACCEL`), `SFX_BULLET_MISS`
4. 종료 판정
   - 길이 0 → 레벨 클리어(초록 2회 점멸) → 다음 레벨
   - `headPos` ≤ 코트 끝 → 게임 오버(빨강 3회 점멸). **레벨 1·0점으로 리셋**되고 READY 화면으로 돌아간다

**레벨 곡선**

| 항목 | 값 |
|------|-----|
| 길이 | `5 + (레벨-1) × 3`칸, 상한 100 (레벨 33부터 고정) |
| 속도 | `6.0 + (레벨-1) × 1.0`칸/초, 상한 60 |

**색 규칙** — 스위치 색은 위치 순서대로 Red / Green / Blue / White.
- 레벨 1~9: 흰색 스위치는 꺼지고 눌러도 발사되지 않으며, 지렁이에도 흰색이 섞이지 않는다(3색)
- 레벨 10: 흰색 해금 스윕 연출 후부터 4색
- 레벨 20+: 매 레벨 시작 전 4색을 스위치에 무작위 재배정(Fisher-Yates)

레벨 시작 연출은 셋 중 하나다. 흰색 해금(레벨 10) / 색 셔플 점멸(레벨 20+) /
3-2-1-GO 카운트다운(그 외). 연출 구간은 원본 타이밍을 그대로 옮기려고 `delay()`로 막고 진행한다.

### 2.2 Solo Pong

**상태**
- `ballPos` / `ballSpeed` / `ballDir`(+1 = CPU 쪽, -1 = 플레이어 쪽)
- 플레이어 영역 = 코트 인덱스가 작은 쪽 끝 18칸, CPU 영역 = 반대쪽 끝 18칸

**루프**
1. 공 이동: `ballPos += ballDir * ballSpeed * dt`
2. 플레이어 판정: 공이 자기 쪽으로 오는 중이고 자기 영역 안에 있을 때 아무 스위치나 누르면 반사.
   영역 밖에서 누르면 헛스윙으로 아무 일도 없다(페널티 없음)
3. CPU 판정: 확률로 반사하는 게 아니라, 공이 자기 영역에 들어온 순간 **"언제 스윙할지"를 한 번 정하고**
   그 시각에 스윙한다. 그때 공이 이미 지나갔으면 놓친 것이다 — 미스는 주사위가 아니라 느린 반응의 결과다.
   공이 빠를수록 영역 통과 시간이 짧아져 같은 반응으로도 더 자주 놓친다
   (18칸 통과 시간: 서브 60칸/초 → 300ms, 100칸/초 → 180ms, 상한 220칸/초 → 82ms)
4. 공이 코트 밖으로 나가면 반대쪽 득점

**반사 속도** — 친 위치(영역 안에서 공이 어디까지 들어왔는지)로 결정된다. 연속적인 정확도 값이 아니다.

| 친 위치 | 배율 | 비고 |
|---------|------|------|
| 초록(가장 일찍, 안쪽 9칸) | ×1.00 | 속도 유지 |
| 노랑(중간 6칸) | ×1.18 | **가장 크게 가속** |
| 빨강(아슬아슬, 끝 3칸) | ×1.10 | 최저 85칸/초 보장 |

상한은 220칸/초. **CPU의 리턴은 속도를 바꾸지 않는다** — 랠리를 가속하는 건 플레이어의 타이밍뿐이다.
공은 항상 흰색 1칸이고(빠를 때 잔상을 남긴다), 색이 바뀌지는 않는다. 코트에 그려지는 색은 타격 영역 표시다.

**서브** — 1인용이라 서브권 교대가 없다. 항상 코트 중앙에서 플레이어 쪽으로 60칸/초로 출발한다.

**승패** — 라이프 개념은 없다. 플레이어가 5점을 먼저 내면 레벨 클리어(레벨 상한 없음),
CPU가 5점을 먼저 내면 게임 오버(레벨 1부터 다시).

**CPU 난이도** — 레벨이 오를수록 세 값이 같은 모양으로 깎인다(`rampDown`).

| 파라미터 | 레벨 1 | 레벨당 | 최소 |
|----------|--------|--------|------|
| 반응 지연 | 120ms | -10ms | 20ms |
| 반응 흔들림(±) | 60ms | -5ms | 8ms |
| 무반응 확률 | 20% | -2%p | 2% |

### 2.3 Duel Pong

공 물리와 반사 규칙(영역 18칸, 초록/노랑/빨강 배율, 상한 220)은 Solo Pong과 같은 값이지만,
**코드를 공유하지는 않는다.** 두 모듈이 각자 상수와 `reflectSpeed()` / `drawBall()` /
`drawZones()` / `drawHalves()`를 따로 들고 있고, 실제로 공유하는 것은 `Arcade.h`의
좌표·존 계산 헬퍼뿐이다.

**보드 두 대** — 이 게임만 하드웨어가 두 대다.
- 라인 LED가 물려 있는 보드가 **1P이자 authoritative**다. 공 물리·판정·점수·사운드를 혼자
  결정하고 코트를 그린다. 반대 보드는 2P로, 자기 입력을 보내고 받은 상태를 LCD에 그린다
- 역할은 `BOARD_ID_PIN`(5번 토글)으로 정해지고 `begin()`에서 매번 다시 읽으므로 재부팅 없이 바뀐다
- 두 보드가 같은 역할이면 LCD에 `ROLE MISMATCH`가 뜬다
- 페어링 전에는 브로드캐스트 HELLO를 500ms마다 뿌리며 서로를 찾는다(`SEARCHING...`)

**패킷** — `MSG_HELLO` / `MSG_HELLO_ACK` / `MSG_INPUT`(2P→1P) / `MSG_STATE`(1P→2P, 120ms 주기 +
변화 시 즉시) / `MSG_SFX`(1P→2P). 소리를 1P가 판정해 내려보내는 이유는, 2P가 눌렀을 때 그게
유효한 반사인지 헛스윙인지는 1P만 알기 때문이다.

**소리 분리** — 자기 행동의 결과만 자기 쪽에서 들린다.
READY 누름 / 반사 / 놓침은 그 사람만, 준비 완료 / 카운트다운 / START / 서브는 양쪽에서 난다.

**진행**
- 양쪽이 다 READY를 눌러야 시작한다(`OK!` → 3-2-1 카운트다운 → `START`)
- 자기 쪽으로 오는 공만 받아칠 수 있다(방금 친 공을 되받는 것을 막는다). 영역 밖 press는 헛스윙
- **10점 선취 승리**. 첫 서브 방향은 무작위이고, 이후로는 **점수를 잃은 쪽으로** 서브한다(교대가 아니다)
- 점수는 LCD에만 표시한다. 라인 LED는 득점 시 코트 절반을 초록/빨강으로 나눠 보여준다
- 승리 화면은 두 보드가 서로 다른 글자를 띄우고(`YOU WIN` / `YOU LOSE`), 5초 뒤 무지개로 바뀐다

---

## 3. 개발 예정 (백로그)

### Solo
- Line Wobbler — 조이스틱 이동 + 흔들기 공격, 장애물 회피 (조이스틱 필요)
- 타이밍 스톱 — 왕복 LED를 목표 존에서 정지, 성공할수록 존 축소
- 반응속도 테스트 — 무작위 점등 후 반응 시간을 LED 길이로 표시
- 회피 러너 — 접근 장애물을 점프 버튼으로 회피 (1D 공룡 게임)
- 두더지 잡기 — 무작위 위치 색 점멸, 해당 색 버튼을 시간 내 타격
- 총알 지옥 — 양방향 접근 적을 색상 매칭 격추 (Snake 확장판)
- 리듬 게임 = 버튼 입력 타이밍에 따라 LED 점멸, 정확도에 따라 점수/콤보
- flappy bird — 버튼으로 , 장애물 회피



### 퍼즐/두뇌
- Simon Says — 색 순서 기억 후 입력, 점점 길어짐
- 1D 테트리스/스택 — 접근 블록을 타이밍 맞춰 쌓기, 어긋난 만큼 잘림
- 색 합성 퍼즐 — 색 혼합(R+G=노랑)으로 목표 색 생성
- 금고 털기 — 순환 LED를 특정 위치에 연속 정지

### 캐주얼
- 1D 룰렛/슬롯 — 감속 회전 LED 정지 위치 + 베팅
- 줄다리기 vs CPU — 연타로 표시등 끌어오기
- 폭탄 돌리기(솔로) — 가속 타이머, 조작 성공 시 시간 연장

### Duel
- 줄다리기 — 양쪽 연타로 경계선 밀기 (구현 최간단)
- 반응속도 대결 — 먼저 누른 쪽 승, 페이크 신호 감점
- 1D 격투 — 이동/공격/가드/대시, 사거리·타이밍 싸움
- 영역 전쟁 — 자기 색으로 칠하며 점유율 승부
- 스네이크 대전 — 같은 라인에서 먹이 경쟁
- 폭탄 돌리기(핫포테이토) — 타이밍 반사, 폭발 시 패배
- 동시 슈팅 대전 — 상호 총알 발사, 색 매칭 요격
 시간마다 쌓이는 tick을 이용해서 총알을 발사하고, 상대방의 색과 같은 색의 총알을 발사해서 맞추면 tick을 번다던가 하는 식으로 전략을 만들 수 있을 것 같다. 
- 리듬 배틀 — 동일 패턴 동시 플레이, 정확도로 방해 요소 전송
- Simon 대전 — 번갈아 시퀀스 추가, 먼저 틀린 쪽 패배
- 결투(서부극) — 무작위 딜레이 후 먼저 쏘는 쪽 승, 3판 2선승

### 시스템/부가
- 아이템 드롭(속도 감소, 라인 확장), 보스전(체력바), 콤보 배율
- 하이스코어: LED 바 그래프 + NVS 저장 (설정값 저장은 이미 `Settings`로 들어가 있으니 같은 자리에 붙이면 된다)
- 어트랙트 모드: 대기 중 레인보우 데모 — Snake READY 화면에 5초 무입력 시 동작하도록 들어가 있다. 메뉴 전체로 넓히는 것이 남았다
- 입력 다양화: 가속도계(흔들기), 로터리 엔코더, 조이스틱

---
