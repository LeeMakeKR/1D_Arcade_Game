# 1D Arcade Game — 게임 정리

## 1. 확정 게임 (v1 구현 대상)

| 구분 | 게임 | 요약 |
|------|------|------|
| 1P | Snake (지렁이 슈팅) | 다가오는 멀티컬러 지렁이를 색 매칭 버튼으로 격추 |
| 1P | Solo Pong | 타이밍 버튼으로 LED 공을 반사, 정확할수록 공이 빨라짐 |
| 2P | Duel Pong | 양 끝에서 두 명이 공을 주고받는 대전 |

---

## 2. 확정 게임 알고리즘

### 공통 구조
- 모든 오브젝트는 1D 스프라이트: `{ 위치(float), 길이, 색 패턴, 속도, 레이어 }`
- 게임은 `init() / update(dt, input) / render(buffer)` 인터페이스만 구현
- 게임은 이벤트만 발생: `SHOOT, HIT, MISS, BOUNCE, GAME_OVER, GAME_CLEAR` → 사운드/이펙트는 별도 매니저가 처리
- 고정 타임스텝 업데이트, 렌더러는 60~100Hz로 프레임버퍼를 스트립에 전송(RMT 논블로킹)

### 2.1 Snake (지렁이 슈팅)

**상태**
- `snake`: 세그먼트 큐(각 세그먼트 = 색상, 버튼 색 3~4개 중 하나)
- `head_pos`: float, 플레이어 방향으로 `snake_speed`씩 전진
- `bullets[]`: `{ pos, color, speed }` (플레이어 → 지렁이 방향)

**루프**
1. 입력: 색 버튼 press → 해당 색 총알 생성, `SHOOT` 이벤트
2. 지렁이 전진, 총알 전진
3. 충돌 판정: 총알 pos ≥ 지렁이 선두 세그먼트 pos
   - 색 일치 → 선두 세그먼트 제거, 점수+, `HIT`
   - 색 불일치 → 총알 소멸(또는 페널티: 지렁이 가속), `MISS`
4. 종료 판정
   - 지렁이 길이 0 → 웨이브 클리어(`GAME_CLEAR`), 다음 웨이브는 더 길고 빠름
   - `head_pos` ≤ 플레이어 존 → `GAME_OVER`

**난이도 변수**: 지렁이 속도·길이, 색 수(3→4), 지렁이 역총알(추가 난이도)

### 2.2 Solo Pong

**상태**
- `ball`: `{ pos(float), vel(±speed) }`
- 플레이어 존: 스트립 한쪽 끝 N개 LED
- CPU 존: 반대쪽 끝

**루프**
1. 공 이동: `pos += vel * dt`
2. 플레이어 측 판정: 공이 플레이어 존 안에 있을 때 버튼 press
   - 정확도 = 최적 반사점과 press 시점 거리 → 정확할수록 `speed *= boost`, CPU 미스 확률 증가
   - 존 내 press 실패(공 통과) → `MISS` → 라이프 감소 or 게임오버
3. CPU 측: 공 도착 시 확률 `p(난이도, 공 속도)`로 반사. 미스 → 플레이어 점수+
4. 반사 시 `BOUNCE` 이벤트, 정확도에 따라 공 색상 변화(선택)

**난이도 변수**: 초기 공 속도, CPU 반사 확률, 플레이어 존 크기, 속도 증가율

### 2.3 Duel Pong

Solo Pong에서 CPU를 제거하고 양 끝 모두 플레이어 존으로 치환.

- 판정: 자기 존에서 타이밍 press 성공 시 반사, 실패(통과 또는 존 밖 press) 시 상대 득점
- 정확한 반사일수록 공 가속 → 속도가 올라갈수록 타이밍 윈도우가 좁아지는 자연 난이도 곡선
- 서브 교대, N점 선취 승리(예: 5점)
- press 시점 정확도로 반사 속도 차등: 퍼펙트=가속, 보통=유지, 아슬아슬=감속(선택 규칙)
- 스트립 중앙에 점수 표시 구간 또는 LCD에 점수 표시

**Solo/Duel Pong 공유 코드**: 공 물리, 반사 판정, 정확도 계산은 동일 모듈. 차이는 한쪽 끝의 입력 소스(CPU vs 플레이어2)뿐.

---

## 3. 개발 예정 (백로그)

### Solo
- Line Wobbler — 조이스틱 이동 + 흔들기 공격, 장애물 회피 (조이스틱 필요)
- 타이밍 스톱 — 왕복 LED를 목표 존에서 정지, 성공할수록 존 축소
- 반응속도 테스트 — 무작위 점등 후 반응 시간을 LED 길이로 표시
- 회피 러너 — 접근 장애물을 점프 버튼으로 회피 (1D 공룡 게임)
- 두더지 잡기 — 무작위 위치 색 점멸, 해당 색 버튼을 시간 내 타격
- 총알 지옥 — 양방향 접근 적을 색상 매칭 격추 (Snake 확장판)
- 리듬 게임 = 버튼 입력 타이밍에 따라 LED 점멸, 정확도에 따라 점수/콤보
- flappy bird — 버튼으로 , 장애물 회피



### 퍼즐/두뇌
- Simon Says — 색 순서 기억 후 입력, 점점 길어짐
- 1D 테트리스/스택 — 접근 블록을 타이밍 맞춰 쌓기, 어긋난 만큼 잘림
- 색 합성 퍼즐 — 색 혼합(R+G=노랑)으로 목표 색 생성
- 금고 털기 — 순환 LED를 특정 위치에 연속 정지

### 성장/전략
- 1D 스네이크(원조) — 먹이 먹고 길어지며 생존
- 타워 디펜스 — 적 웨이브를 색상 매칭으로 방어?
- 낚시 — 훅을 내려 지나가는 물고기를 타이밍 포획

### 캐주얼
- 1D 룰렛/슬롯 — 감속 회전 LED 정지 위치 + 베팅
- 줄다리기 vs CPU — 연타로 표시등 끌어오기
- 폭탄 돌리기(솔로) — 가속 타이머, 조작 성공 시 시간 연장

### Duel
- 줄다리기 — 양쪽 연타로 경계선 밀기 (구현 최간단)
- 반응속도 대결 — 먼저 누른 쪽 승, 페이크 신호 감점
- 1D 격투 — 이동/공격/가드/대시, 사거리·타이밍 싸움
- 영역 전쟁 — 자기 색으로 칠하며 점유율 승부
- 스네이크 대전 — 같은 라인에서 먹이 경쟁
- 폭탄 돌리기(핫포테이토) — 타이밍 반사, 폭발 시 패배
- 동시 슈팅 대전 — 상호 총알 발사, 색 매칭 요격
- 리듬 배틀 — 동일 패턴 동시 플레이, 정확도로 방해 요소 전송
- Simon 대전 — 번갈아 시퀀스 추가, 먼저 틀린 쪽 패배
- 결투(서부극) — 무작위 딜레이 후 먼저 쏘는 쪽 승, 3판 2선승

### 시스템/부가
- 아이템 드롭(속도 감소, 라인 확장), 보스전(체력바), 콤보 배율
- 하이스코어: LED 바 그래프 + EEPROM 저장
- 어트랙트 모드: 대기 중 레인보우 데모
- 입력 다양화: 가속도계(흔들기), 로터리 엔코더, 조이스틱

---

## 4. 사운드 목록 (v1)
- GAME START / GAME OVER / GAME CLEAR
- 총알 발사, 타격(HIT), 피격/미스
- Pong 반사(BOUNCE), 퍼펙트 반사(별도 톤)
- 지렁이 전진음(루프성, 속도 연동 피치 변화 검토)